# v129 改修進捗

基準：`class-support-app_v129_release_device-unverified.zip`  
正本：ユーザー提供の `CLASS_SUPPORT_APP_V129_RENOVATION_ROADMAP.md`

## R001 変更前の回帰基準を保存

状態：✅ 完了

- 変更前に既存6件の回帰テストとJavaScript構文解析33本を実行し、成功した。
- テストの保証範囲と非保証範囲を `R001_REGRESSION_BASELINE.md` に記録した。

## R101 表示設定画面の実行時エラーを修正

状態：🔍 実機確認待ち

変更：

- `.panel h1` を必須参照する旧処理を撤去した。
- 表示設定の入力読取り、イベント登録、アイコン切替、保存操作を `#appearance-form` 内へ限定した。
- 最小DOMで表示設定の描画処理を実行し、例外なく変更・送信イベントが登録されることを検証する `appearance-settings-render.test.js` を追加した。

検証：

- 回帰テスト7件：成功
- JavaScript構文解析34本：成功
- PC/iPad Safariでの開閉・全項目変更・再表示：未確認

## R102 クラス削除の説明と実処理を一致させる

状態：🔍 実機確認待ち

変更：

- クラス削除を完全削除から30日間のごみ箱移動へ変更した。
- ごみ箱へクラス、名簿、記録、削除対象の児童を一つの復元用バンドルとして保存する。
- 最後のクラスを削除しても `resetAll()` を実行せず、年度・認証・他年度データを保持する。
- ごみ箱画面でクラスを識別し、名簿・記録件数を表示してまとめて復元できるようにした。

検証：

- `class-deletion-regression.test.js` を追加し、削除バンドル形式、復元経路、全消去への誤遷移がないことを検証した。
- 回帰テスト8件：成功
- JavaScript構文解析35本：成功
- 複数年度DB・実ブラウザでの削除と復元：未確認

## R103 同期の削除履歴と削除承認を一体化する

状態：🔍 実機確認待ち

変更：

- 同期計画の通常書き込み対象から `trash` を外し、削除候補の判定を `syncDeletionCandidate()` へ分離した。
- 受信した削除tombstoneは削除候補としてだけ保持し、削除未承認では記録・ごみ箱とも書き換えないようにした。
- 削除承認時だけ、tombstoneの保存と対象記録の削除を同じ `applyBatch` で実行するようにした。
- 対象記録がすでにない削除tombstoneも、承認時に保存して再同期での復活を防げるようにした。
- 週宿題・単発宿題・日次記録・成績入力の取消操作も、ごみ箱へ移す共通経路に統一した。

検証：

- `sync-deletion-regression.test.js` を追加し、削除候補、古いtombstone、新しいローカル記録、不正な削除情報、承認の有無によるバッチ内容を検証した。
- 回帰テスト9件：成功
- JavaScript構文解析36本：成功
- 実DBでの二端末往復同期と、削除候補を承認・未承認にした場合の画面確認：未確認

## 次の対象

## R104 同一更新時刻の競合を失わない

状態：🔍 実機確認待ち

変更：

- 同一ID・同一更新時刻で内容が異なる記録を、上書きもスキップもせず「要確認」の競合コピーとして追加するようにした。
- 競合コピーには元ID、取込元端末、`sameUpdatedAt` の理由、要確認フラグを保存する。
- 同期プレビューの詳細画面に、要確認件数と元の記録を残す動作を表示する。

検証：

- `sync-conflict-regression.test.js` を追加し、同時刻・内容差異、時刻差、完全一致の3ケースと競合コピーの保存情報を検証した。
- 回帰テスト10件：成功
- JavaScript構文解析37本：成功
- 実端末間で同時編集した記録の同期プレビューと、要確認コピーの確認：未確認

## 次の対象

## R105 端末設定と年度共通設定の同期範囲を分離

状態：🔍 実機確認待ち

- 年度間で共有するmetaキーを明示的な許可リストにした。成績基準、児童表示、記録タグ、所見テンプレートなどだけを同期・バックアップの対象にする。
- テーマ、一覧密度、端末名、PIN省略、選択クラス、画面遷移、同期履歴など端末専用情報は同期ファイルから除外する。
- `sync-meta-scope-regression.test.js` を追加し、年度共通キーと端末専用キーの境界を検証した。
- 回帰テスト11件、JavaScript構文解析38本：成功。
- PC/iPad間でテーマ・PIN省略が変化せず年度共通設定が反映されること：実機確認待ち。

## 次の対象

## R106 同期・復元後にstateをDBから再構築

状態：🔍 実機確認待ち

- 表示設定、児童表示、フッター、選択クラス、年度、バックアップ・同期日時をDBから再読込する `reloadStateFromDb()` を追加した。
- 同期適用、同期前復元、バックアップ復元、復旧用バックアップ置換の完了後に共通処理を実行するよう統一した。
- `state-reload-regression.test.js` を追加し、必要な設定の再読込と全置換経路への接続を検証した。
- 回帰テスト12件、JavaScript構文解析39本：成功。
- 設定が異なる実バックアップの復元直後とページ再読み込み後の表示一致：実機確認待ち。

## 次の対象

R107の年度範囲を限定した復元処理。

## R110 小テスト変更の確認文と点数保持

状態：🔍 実機確認待ち

- 種類変更で入力済み点数を消去せず、確認文どおり保持するよう修正した。
- 回帰テストで確認文と消去処理が一致しないことを検証した。

## R201・R203 表示設定のスコープと情報量

状態：🔍 実機確認待ち

- 表示設定をフォーム内参照で保存し、設定変更時にテーマを即時適用する経路を回帰テストで固定した。
- 情報量モードを現行設定カードと操作案内にも適用した。

## R501・R502 外部取込の名寄せとCSV解析

状態：🔍 実機確認待ち

- 氏名と出席番号が別児童を指す場合を要確認にした。
- 引用符、改行、エスケープ引用符を含むCSVを解析し、閉じない引用符をエラーにする。

## R602・R603 描画後置換とService Worker

状態：🔍 実機確認待ち

- MutationObserverによる描画後の文言置換を撤去した。
- Service Workerは本アプリの旧キャッシュだけを削除する。

## ロードマップ監査（継続中）

未追跡項目は、実装済みとの推定をせず、順番にコード・自動検証・実機確認待ちを判定する。実機確認のR701〜R703は、PC/iPadの実端末または相当環境で確認できるまで実機確認待ちとする。

## R107 同期前復元・バックアップ復元の年度範囲を安全にする

状態：🔍 実機確認待ち

- `ClassDB.replaceYearRaw()` により、対象年度のクラス・在籍・記録・ごみ箱だけを置換する。
- 通常バックアップ復元と同期前復元は年度単位の置換に変更し、障害復旧用の全データ置換とは分離した。
- `year-restore-scope-regression.test.js` で年度置換経路を検証する。

## R108 下書き保護を保存成功と連動させる

状態：🟡 実装継続中

- submit時に一律で下書きを解除する処理を撤去し、ダイアログ内フォームも下書き対象にした。
- 表示設定の保存成功時には `completeFormDraft()` を呼ぶ。
- 全フォームの保存成功経路への適用と、取消時のダイアログ確認は未完了。

## R109 教師認証ロック時に教師用ダイアログを閉じる

状態：🔍 実機確認待ち

- `lockTeacherSession()` で教師秘密・同期保留・下書きを破棄し、ダイアログを閉じて児童画面へ移す。
- `teacher-lock-regression.test.js` でロック経路を検証する。

## R202・R205 表示設定の即時反映と二重構造の解消

状態：🔍 実機確認待ち

- 表示設定はフォーム内の変更を自動保存し、テーマ・情報量・一覧密度・児童表示・アイコンをその場で適用する。
- 実設定の前にあった「何を変えますか」入口カードを撤去し、最初の画面で実際の設定を変更できる構造にした。
- 保存成功時だけフォーム下書きを完了として扱う。

## R302・R304・R305 日常入力画面の圧縮

状態：🔍 実機確認待ち

- 毎日宿題の欠席設定と達成条件を折りたたみ、児童一覧を優先した。
- 週宿題の過去週・管理案内を折りたたみ、選択中の宿題と児童一覧を上へ寄せた。
- 横幅700px以上では児童記録のカテゴリーを3列で表示する。

## R108 下書き保護

状態：🟡 実装継続中

- ダイアログフォームも共通の入力検知対象にし、キャンセル・Escでは未保存確認を行う。
- ダイアログを保存成功で閉じるときだけ下書きを解除する。
- 各画面固有の保存失敗注入と全フォームの実ブラウザ確認が残る。

## R504 不明な取込状態

状態：🔍 実機確認待ち

- 日次以外で解釈できない「忘れた」「一部忘れた」を null として保存せず、保存前にエラーにする。
- 確定処理は非同期保存の失敗を通知する。

## R602 ヘルプ文章の描画後置換を撤去

状態：🔍 実機確認待ち

- ヘルプ本文の用語を生成元へ直接反映し、描画済みDOMを走査して書き換える関数を削除した。
- help-source-regression.test.js で描画後置換が復活しないことを検査する。

## R303 毎日宿題の未確認・未提出の分離

状態：🔍 実機確認待ち

- 日次の未確認と、週宿題・提出物の未提出を状態ラベルとCSV出力で区別することを回帰テストで固定した。

## R601 削除・保存・再描画の共通化

状態：🔍 実機確認待ち

- 記録の削除に直接の records remove を使わず、ごみ箱へ移す共通経路へ統一したことを検査する。

## R704 リリース整合と判定

状態：🔍 実機確認待ち

- 回帰テスト、JavaScript構文検査、配布用フォルダーとZIP、引き継ぎ書を作成した。
- PC・iPad・二台同期の実機確認が未完了のため、正式リリース判定は保留する。

## R401 設定画面の共通骨格

状態：🔍 実機確認待ち

- 設定画面の先頭情報と導線カードを共通部品に集約し、表示設定・データ管理が共通カードを使うことを回帰テストで固定した。

## R204 児童一覧の大きさを教師用・児童用で一貫させる

状態：🔍 実機確認待ち

- 児童用一覧も教師用と同じ `rosterDensity` 設定を参照し、自動・一覧優先・読みやすくの3モードを適用する。
- `roster-density-scope.test.js` で児童用一覧への設定接続を検証する。

## R305 児童記録のカテゴリーカードを圧縮

状態：🔍 実機確認待ち

- 件数があるカテゴリーだけに当日の人数を表示し、横幅700px以上では3列レイアウトを使う。

## R301・R306・R307 日常画面の共通骨格・児童用表示・固定フッター

状態：🔍 実機確認待ち

- 教師用日常画面は共通シェルで主操作と児童一覧を先に表示する。
- 児童用達成アイコンの配置と、固定フッターの余白・安全領域は回帰テストで固定する。

## R404 設定一覧の情報密度

状態：🔍 実機確認待ち

- 設定導線を共通のカードグリッドへ集約し、実機で横向き表示を確認する。

## R503・R603・R604 取込履歴・キャッシュ・依存表

状態：🔍 実機確認待ち

- 取込済み判定にクラスIDを含め、同じ表を別クラスへ安全に取り込めるようにする。
- Service Workerは本アプリ接頭辞の旧キャッシュだけを削除する。
- 依存表はv129の実装位置とテストを参照するよう更新した。

## R605 開発資料を一つの正本へ統合

状態：🔍 実機確認待ち

- ロードマップ原本を V129_ROADMAP.md として同梱し、開発資料の入口から参照できるようにした。

## R402 クラス編集の描画後DOM加工を撤去

状態：🔍 実機確認待ち

- クラス編集は初期HTMLで一般級・個別支援級の入力を生成し、cardifyClassEditor のような描画後加工を含まないことを回帰テストで固定した。

## R403 データ管理の新旧カード混在を解消

状態：🔍 実機確認待ち

- データ管理の補助カードは data-card-role、操作分類は data-data-section を使い、見出し文言に依存しない経路を回帰テストで固定した。
