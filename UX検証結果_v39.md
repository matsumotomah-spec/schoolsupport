# v39 UX検証結果

## 結論

教員が日常的に使う「ホーム→機能→児童を記録」の動線を優先し、設定やデータ操作は目的から選べる構成へ整理した。席替え印刷は、操作画面を印刷する仕組みではなく、座席表だけを生成する専用画面へ変更した。

## 確認した利用場面

| 利用場面 | v39での確認内容 | 結果 |
|---|---|---|
| 教師ホーム | ホームの現在地、現在のクラス、主要機能、要対応人数が区別できる | 適合 |
| 設定を探す | 設定トップから「最初の準備／日常の設定／データ・年度」を目的で選べる | 適合 |
| 初回準備 | クラス・児童を最初に示し、児童未登録も設定トップで判別できる | 適合 |
| 36人の記録 | 自動・読みやすく・一覧優先の表示密度を選べる | 適合 |
| iPad操作 | 主なボタンを44px以上とし、520px・700px幅で折り返す | 適合 |
| 席替え印刷 | 児童側／教師側を選び、座席表以外を生成しない | 適合 |
| 印刷用通路 | 氏名欄の約半分幅。教師側では通路位置も左右反転する | 適合 |
| ヘルプ | 初心者用の作業順、業務別FAQ、ページ内ヘルプを利用できる | 適合 |
| キーボード操作 | フォーカス位置を輪郭で確認できる | 適合 |
| ダーク表示 | ヘッダーを面全体のクラス色から落ち着いた面色＋識別帯へ変更 | 適合 |

## 自動検証

- JavaScript全ファイルの構文検査：合格
- 分割スクリプトの依存順評価：合格
- 主要機能・同期・復旧・CSV・席替えアルゴリズムの回帰テスト：合格
- 教師側印刷で、座席・空席・通路を180度変換するテスト：合格
- 印刷専用画面と旧印刷モードの除去：合格
- `index.html`、Service Worker、全公開資材のv39整合：合格
- ローカルHTTP配信で全公開ファイルが取得できること：合格
- CSSの括弧対応、520px・700pxのレスポンシブ規則：合格

## 公開後に実機で確認する項目

ブラウザの印刷処理と学校端末固有の挙動は自動検査だけでは確定できないため、GitHub Pagesへ配置後に次を確認する。

1. iPad Safariの横向きで、36人の一覧と下部ナビが重ならないこと。
2. PCの印刷プレビューで、席替えの作成手順・設定・履歴・フッターが出ないこと。
3. 児童側／教師側の双方で、黒板の位置と窓側・廊下側が想定どおりであること。
4. 8列×5行で、通路が氏名欄のおよそ半分幅になること。
5. GitHub Pagesの設定→データ管理に「公開版 39」と表示されること。

## 判定

コードと自動検証の範囲ではv39をリリース可能と判定する。実データ投入前に、ダミークラスを使って上記5項目だけ実機確認する。
