# クラス支援ツール v129 改修ロードマップ

## 実行継続・報告制御の必須規約

- 作業担当者は、ロードマップの途中項目、個別テスト、調査、実装の区切りで自ら実行ターンを終了させてはならない。
- R001からR704までを順番に実装・検証し、実機確認以外の作業、回帰テスト、構文検査、リリース整合、引き継ぎ成果物を完了するまで、利用者への進捗報告・完了報告を行わない。
- 各項目の結果、失敗、未確認事項は作業用の進捗記録へ記載し、利用者への報告の代わりとする。
- 実機が必要な項目は「実機確認待ち」と明記する。ただし、それを理由に残る実装・自動検証・資料整備を止めてはならない。
- 最終報告は、ロードマップ全項目の完了監査と成果物の確認を終えた時点で一度だけ行う。

作成日: 2026-09-22  
基準版: `class-support-app_v129_release_device-unverified.zip`  
現在の判定: **v129はリリース候補（RC）。正式リリース保留**

## 1. このロードマップの目的

v129で確認されたデータ保護・同期・表示設定・画面構成・入力操作の問題を、既存データとの互換性を維持しながら段階的に解消する。実装は、データ消失や同期不整合につながる問題を最優先とし、その後にiPadで毎日使う画面の操作性、設定画面の統一、データ取り込み、保守性を改善する。

## 2. 状態の表記

| 状態 | 意味 |
|---|---|
| ✅ 完了 | 実装、必要な自動テスト、対象端末での確認まで完了 |
| 🟡 着手 | 調査または実装を開始したが、完了条件を満たしていない |
| ⬜ 未着手 | 調査結果はあるが、コード変更を開始していない |
| 🔍 実機確認待ち | 実装・自動テストは完了し、PCまたはiPadでの確認が残る |
| ⛔ 保留 | 先行タスクまたは仕様決定を待つ |

「コードを変更した」だけでは完了にしない。各項目に記載した受け入れ条件と検証を満たした時点で完了とする。

## 3. 全体進捗

| 段階 | 優先度 | 内容 | 状態 |
|---|---:|---|---|
| 0 | P0 | 調査結果と互換境界の固定 | ✅ 完了 |
| 1 | P0 | 画面停止・データ消失・同期不整合の修正 | ⬜ 未着手 |
| 2 | P1 | 表示設定の保存・即時反映・適用範囲の修正 | ⬜ 未着手 |
| 3 | P1 | 毎日使う入力画面を児童一覧中心に再構成 | ⬜ 未着手 |
| 4 | P1 | 設定・データ管理画面の共通化 | ⬜ 未着手 |
| 5 | P2 | CSV・Excel取り込みの安全性向上 | ⬜ 未着手 |
| 6 | P2 | コード構造・ヘルプ・資料の整理 | ⬜ 未着手 |
| 7 | P0判定 | PC・iPad・2台同期の実機検証と正式リリース | ⛔ 保留 |

---

## 段階0: 調査結果と互換境界の固定

### R000 v129の現状確認

- **優先度:** P0
- **状態:** ✅ 完了
- **確認済み:** 回帰テスト6件成功、全JavaScript構文検査成功、v129の版番号とService Worker資材番号の一致
- **実機資料:** iPad横向きの設定、表示設定、同期、児童記録、毎日の宿題、週宿題、児童用画面
- **固定する互換境界:** IndexedDBのDB名・ストア名・キー、暗号化封筒v1/v2、同期ペイロードv1、旧ノート履歴のsessionKey

### R001 変更前の回帰基準を保存

- **優先度:** P0
- **状態:** ⬜ 未着手
- **対象:** `tests/`、`README.md`、`JS_DEPENDENCY_MAP.md`
- **作業:** 現在の6テストを変更前に実行し、今後追加するテストの目的と対象仕様を一覧化する。
- **完了条件:** 既存6テストが成功し、テストが守る仕様と守らない仕様が文書化されている。

---

## 段階1: 画面停止・データ消失・同期不整合の修正

### R101 表示設定画面の実行時エラーを修正

- **優先度:** P0
- **状態:** ⬜ 未着手
- **対象コード:** `app-settings-display.js` の `renderAppearanceSettings()`、`wireDisplaySettingsGuide()`
- **問題:** 存在しない可能性が高い `.panel h1` を取得して `textContent` を変更しており、画面描画の途中で停止する。目的カードも `.settings-option-card` を前提にしているが、現在のHTML構造と一致しない。
- **作業:** DOM構造を先に確定し、後から見出しやカードを探して加工する処理を廃止する。表示設定の初期描画とイベント登録を一つの構造にそろえる。
- **完了条件:** 表示設定を開いてエラーが発生せず、全項目を変更でき、戻る・再表示後も操作できる。
- **検証:** 表示設定の描画テスト、イベント登録テスト、PC/iPad Safariでの開閉確認。

### R102 クラス削除の説明と実処理を一致させる

- **優先度:** P0
- **状態:** ⬜ 未着手
- **対象コード:** `app-data.js` の削除カード、`app-data-migration.js` の `confirmDeleteCurrentClass()`、`resetToWelcomePreservingLegacy()`
- **問題:** 設定画面は「ごみ箱へ移す」と説明する一方、実際はクラス・名簿・記録を完全削除する。最後のクラスを削除すると全ストア初期化へ進み、他年度データも消える可能性がある。
- **作業:** クラス削除を30日間復元可能な削除へ変更するか、完全削除として説明・確認・対象範囲を明示する。最後のクラス削除と全データ削除を別処理にする。
- **完了条件:** クラス削除で他年度データが変化せず、画面の説明と保存結果が一致する。復元可能と表示する場合は実際に復元できる。
- **検証:** 複数年度・1クラス・複数クラスの削除テスト、削除前後の全ストア比較。

### R103 同期の削除履歴と削除承認を一体化する

- **優先度:** P0
- **状態:** ⬜ 未着手
- **対象コード:** `app-data-sync.js` の `buildSyncPlan()`、`openSyncPreview()`、`applySyncPlan()`、`app-shell.js` の `trashEntryFor()` と `moveToTrash()`
- **問題:** 削除を承認しなくても、受信したごみ箱データが通常の書き込みとして保存される。直接 `ClassDB.remove()` する取り消し・欠席解除・小テスト取消は削除履歴を残さず、別端末から古い記録が戻る可能性がある。
- **作業:** 同期対象の削除をtombstone（削除記録）として統一し、承認時だけ記録削除と削除履歴を同時適用する。同期対象になり得る記録の取消は、原則として共通削除関数を通す。
- **完了条件:** 削除未承認では記録も削除履歴も変更されず、承認時には両方が整合する。別端末との再同期で削除済み記録が復活しない。
- **検証:** PC→iPad→PCの往復同期、削除承認あり・なし、取消後の再同期テスト。

### R104 同一更新時刻の競合を失わない

- **優先度:** P0
- **状態:** ⬜ 未着手
- **対象コード:** `app-data-sync.js` の `newerThan()`、`buildSyncPlan()`、同期プレビュー
- **問題:** 同じ `updatedAt` で内容が異なる記録はスキップされる。現在の「差異は両方残す」は更新日時がない旧記録にしか適用されない。
- **作業:** 同一時刻・内容差異を競合として検出し、双方を残すかユーザーに選択させる。競合コピーには元ID、端末、競合理由を保存する。
- **完了条件:** 同一時刻で内容が異なる記録が消えず、同期プレビューに「要確認」として表示される。
- **検証:** 同一ID・同一時刻・異なる内容、時刻が新旧、完全一致の3ケース。

### R105 端末設定と年度共通設定の同期範囲を分離

- **優先度:** P0
- **状態:** ⬜ 未着手
- **対象コード:** `app-data-crypto.js` の `collectYearPayload()`、`app-data-sync.js` の `buildSyncPlan()`、設定metaキー定義
- **問題:** 「この端末」と表示されるテーマ、一覧密度、端末名、PCのPIN省略設定などが同期対象になっている。
- **作業:** metaキーを「端末専用」「年度共通」「全年度共通」に分類し、同期・バックアップ・復元の対象を明示的な許可リストで決める。
- **完了条件:** PCの表示・PIN省略設定をiPadへ取り込んでもiPad側が変化しない。年度共通設定は期待どおり同期される。
- **検証:** metaキーごとの同期対象テスト、PC/iPad間の実同期。

### R106 同期・復元後にstateをDBから再構築

- **優先度:** P0
- **状態:** ⬜ 未着手
- **対象コード:** `app-core.js` の `loadState()`、`app-data-sync.js` の `applySyncPlan()`、`app-data-crypto.js` の `restorePreSyncSnapshot()` と `restoreBackupFile()`
- **問題:** 同期・復元後は年度・クラスを中心に更新しており、表示設定、児童用表示、フッター、下書きなどが復元前のメモリー状態で残る可能性がある。
- **作業:** DBを置き換えた後に共通の `reloadStateFromDb()` 相当を実行し、認証情報など残すべきセッション状態だけを明示的に引き継ぐ。
- **完了条件:** 同期・復元直後と再読み込み後の表示・設定・選択クラスが一致する。
- **検証:** 設定が異なるバックアップの復元、同期直後と再読み込み後のstate比較。

### R107 同期前復元・バックアップ復元の年度範囲を安全にする

- **優先度:** P0
- **状態:** ⬜ 未着手
- **対象コード:** `app-data-crypto.js` の `savePreSyncSnapshot()`、`restorePreSyncSnapshot()`、`restoreBackupFile()`、`db.js` の `replaceAllRaw()`
- **問題:** 保存ファイルは現年度中心だが、復元処理は全ストアを置換するため、端末内の他年度データを消す可能性がある。
- **作業:** 現年度だけを置換する処理と、全データ復旧を明確に分離する。他年度を保持したまま対象年度を差し替えるトランザクションを追加する。
- **完了条件:** 2026年度を復元しても2025年度のデータが変化しない。全データ復旧を行う場合だけ全年度置換を明示する。
- **検証:** 2年度を保存したDBに対する年度復元テスト、失敗時のロールバック確認。

### R108 下書き保護を保存成功と連動させる

- **優先度:** P0
- **状態:** ⬜ 未着手
- **対象コード:** `app.js` の共通 `input` / `submit` リスナー、`app-core.js` の `markFormDraftDirty()`、`clearFormDraft()`、`navigateSafely()`、各ダイアログフォーム
- **問題:** `submit` の捕捉時点で未保存状態を解除するため、検証エラーやDB保存失敗でも警告が消える。ダイアログは `#app` 外にあり共通保護の対象外。
- **作業:** 保存処理が成功した箇所でだけ `clearFormDraft()` を呼ぶ。ダイアログを含むフォームに明示的なdraftキーを付ける。閉じる・画面移動・認証ロック時の確認規則を統一する。
- **完了条件:** 入力エラー・保存失敗・ダイアログ終了・画面移動で未保存内容が無言で失われない。
- **検証:** 保存成功、入力検証失敗、DB失敗、キャンセル、ロック発生の各ケース。

### R109 教師認証ロック時に教師用ダイアログを閉じる

- **優先度:** P0
- **状態:** ⬜ 未着手
- **対象コード:** `app-core.js` の `scheduleLock()`、`renderPupil()`、`openDialog()`、`closeDialog()`
- **問題:** 自動ロック時に児童画面へ移動しても、教師用ダイアログが残る可能性がある。
- **作業:** ロック処理を共通化し、教師用ダイアログ・セッション秘密・教師用一時データを閉じてから児童画面へ移動する。
- **完了条件:** どの教師用ダイアログを開いていても、無操作5分後は児童画面だけが表示され、教師用操作を継続できない。
- **検証:** 名簿、設定、小テスト、同期ダイアログを開いた状態でのロック確認。

### R110 小テストの確認文と消去動作を一致させる

- **優先度:** P0
- **状態:** ⬜ 未着手
- **対象コード:** `app-grades.js` の手入力小テスト画面、`confirmQuizChange()`、種類変更イベント
- **問題:** 「入力済みの点数は保持されます」と表示した後、実際には `scores.clear()` で消去する。
- **作業:** 種類変更でも点数を保持できる場合は保持し、消去が必要なら確認文に人数・影響・消去されることを明記する。
- **完了条件:** 確認文どおりの結果になり、キャンセル時は種類・題名・得点が変化しない。
- **検証:** 得点入力後の種類変更、キャンセル、入力方式変更、並び順変更。

---

## 段階2: 表示設定の保存・即時反映・適用範囲の修正

### R201 表示設定の読取・保存をフォーム内に限定

- **優先度:** P1
- **状態:** ⬜ 未着手
- **対象コード:** `app-settings-display.js` の `renderAppearanceSettings()` と `saveAppearanceImmediately()`
- **問題:** `document.querySelector()` で画面全体を検索し、将来同名要素が増えた場合に別フォームを参照する危険がある。
- **作業:** `target` または `appearance-form` を基準に値を読む。保存値を検証し、DBから再読込してstateへ反映する。
- **完了条件:** 各設定を1項目ずつ変更しても他項目が変わらず、保存値とstateが一致する。

### R202 表示設定をその場で再描画する

- **優先度:** P1
- **状態:** ⬜ 未着手
- **対象コード:** `app-settings-display.js`、`app-core.js` の `applyTheme()`、`app-shell.js` の各一覧描画
- **作業:** テーマ、情報量、一覧密度、児童用文字、アイコン変更後に、現在値・プレビュー・説明・アイコンを即時更新する。「保存中」「保存済み」「失敗」を画面内に表示する。
- **完了条件:** 戻る操作や再読み込みなしで違いが見え、再読み込み後も保持される。

### R203 「すっきり・標準・詳しく」の効果を全画面で統一

- **優先度:** P1
- **状態:** ⬜ 未着手
- **対象コード:** `styles.css` の `data-information` / `data-explanations`、`app-shell.js` の `operationTipHtml()`、各画面の説明部品
- **問題:** 現在は旧 `.panel` の一部説明にしか効かず、新しいカードや既に描画された折りたたみに反映されにくい。
- **作業:** 説明を共通クラスまたは共通部品へ統一し、主操作・結果・保存状態は全モードで残す。
- **完了条件:** 3モードの情報量差が明確で、操作結果と重要な警告はどのモードでも消えない。

### R204 児童一覧の大きさを教師用・児童用で一貫させる

- **優先度:** P1
- **状態:** ⬜ 未着手
- **対象コード:** `app-shell.js` の `teacherStudentGrid()`、`pupilStudentGrid()`、`app-behavior.js` の児童グリッド、`styles.css` の `compact-roster`
- **問題:** `rosterDensity` は教師用の一部だけが参照し、児童用は人数による自動判定に固定されている。
- **作業:** 適用範囲を画面上で明示し、対象となる一覧は共通の密度判定を使う。座席形状を保持する画面では列数との関係も定義する。
- **完了条件:** 自動・読みやすく・一覧優先の違いが教師用と児童用で確認でき、29～36人でも名前と状態が読める。

### R205 表示設定の二重構造を解消

- **優先度:** P1
- **状態:** ⬜ 未着手
- **対象コード:** `app-settings-display.js` の `displaySettingsGuideHtml()`、`wireDisplaySettingsGuide()`、`renderAppearanceSettings()`
- **実機根拠:** `IMG_3432.PNG`、`IMG_3433.PNG`
- **問題:** 「何を変えるか」のカードと実設定が重複し、実際の変更まで縦スクロールが必要。「選んだ項目だけ表示」と実動作も一致しない。
- **作業:** 入口カードを廃止して実設定を上から表示するか、カード選択後に対象設定だけを表示する。両方式は併用しない。
- **完了条件:** 表示設定を開いた最初の画面で少なくとも1つの実設定を変更できる。

---

## 段階3: 毎日使う入力画面を児童一覧中心に再構成

### R301 共通の「主操作優先」画面骨格を作る

- **優先度:** P1
- **状態:** ⬜ 未着手
- **対象コード:** `app-shell.js` の各教師用描画、`app-records.js`、`app-grades.js`、`styles.css`
- **作業:** 日常入力画面を「コンパクトなタイトル・日付・並び順」「児童一覧」「必要時に開く補助設定」の順に統一する。
- **共通完了条件:** iPad横向きで画面を開いた時点で、児童一覧の1行目全体が見える。

### R302 毎日の宿題の上部を圧縮

- **優先度:** P1
- **状態:** ⬜ 未着手
- **対象コード:** `app-shell.js` の `renderTeacherDaily()`、毎日宿題の操作説明・集計・欠席設定・達成条件、`styles.css`
- **実機根拠:** `IMG_3436.PNG`
- **作業:** 日付・並び順・集計を1行または2行へまとめる。「押し方」「欠席をまとめて設定」「達成アイコン条件」は折りたたみまたは補助メニューへ移す。
- **完了条件:** 29人・iPad横向きで、初期表示に児童一覧1行目が完全に見える。日付変更と並び替えはスクロールせず操作できる。

### R303 毎日の宿題の「未確認」と「未提出」を分離

- **優先度:** P1
- **状態:** ⬜ 未着手
- **対象コード:** `app-shell.js` の日次状態集計、`dailyNextStatus()`、`pupilStatusLabel()`、ヘルプ文、回帰テスト
- **実機根拠:** `IMG_3436.PNG` では集計が「未提出28」、カードが「未確認」
- **作業:** 業務上の状態定義を一つに固定し、カード・集計・CSV・児童用画面・ヘルプで同じ語を使う。
- **完了条件:** 未確認と未提出が別件数として正しく表示され、状態遷移とCSV出力が一致する。

### R304 週宿題の児童一覧を上へ移動

- **優先度:** P1
- **状態:** ⬜ 未着手
- **対象コード:** `app-records.js` の `renderWeekly()` と週選択・作成・終了管理、`app-shell.js` の週宿題導線、`styles.css`
- **実機根拠:** `IMG_3430.PNG`
- **作業:** 選択中宿題、週選択、並び順をコンパクトな操作領域にまとめる。押し方、過去週説明、表示・終了管理、新規作成は必要時に開く。今週分未作成案内は薄い通知行にする。
- **完了条件:** iPad横向きで児童一覧1行目が完全に見え、現在記録している宿題名と期限が一目で分かる。

### R305 児童記録のカテゴリーカードを圧縮

- **優先度:** P2
- **状態:** ⬜ 未着手
- **対象コード:** `app-behavior.js` のカテゴリー選択画面、`styles.css` の `.behavior-category-card`
- **実機根拠:** `IMG_3435.PNG`
- **作業:** 0件表示を弱くし、件数があるカテゴリーを強調する。説明量モードに応じて説明文を隠し、必要ならiPad横向きで3列表示を検討する。
- **完了条件:** 主操作のカテゴリー選択が最初の画面に収まり、件数のあるカテゴリーを識別しやすい。

### R306 児童用画面の達成アイコン位置を固定

- **優先度:** P2
- **状態:** ⬜ 未着手
- **対象コード:** `app-shell.js` の `studentCard()`、`rewardIconHtml()`、`styles.css` の児童カード・達成アイコン
- **実機根拠:** `IMG_3437.PNG`
- **作業:** 氏名の長さにかかわらずアイコンを右上など同じ位置へ配置し、状態表示と混同しない説明を用意する。
- **完了条件:** 29～36人、長い氏名でも氏名・状態・達成アイコンが重ならず、配置が統一される。

### R307 固定フッターの高さと利用画面を見直す

- **優先度:** P1
- **状態:** ⬜ 未着手
- **対象コード:** `app-shell.js` の教師用フッター生成、`styles.css` の `.teacher-footer` と `.page` の下余白、設定ルート描画
- **実機根拠:** `IMG_3431.PNG`～`IMG_3436.PNG`
- **作業:** 設定、同期、名簿など管理画面では固定フッターを非表示にする。日常入力画面では高さを抑え、最後の児童カードがフッターの上までスクロールできる余白を保証する。
- **完了条件:** 管理画面の操作領域をフッターが覆わず、日常画面の最終行も完全に表示できる。

---

## 段階4: 設定・データ管理画面の共通化

### R401 設定画面の共通骨格を完成

- **優先度:** P1
- **状態:** ⬜ 未着手
- **対象コード:** `app-settings-core.js` の設定カード生成関数、`app-settings-security.js`、`app-settings-classes.js`、`app-data.js`、`styles.css`
- **共通骨格:** ページ名と適用範囲 → 現在値または実設定 → 保存状態 → 次の操作
- **完了条件:** 年度・認証、クラス、表示、記録候補、データ管理で、同じ役割のカードが同じ見た目と操作になる。

### R402 クラス編集の描画後DOM加工を撤去

- **優先度:** P1
- **状態:** ⬜ 未着手
- **対象コード:** `app-settings-classes.js` の `cardifyClassEditor()` と `openClassEditor()`
- **作業:** クラス編集ダイアログを最初から完成したカード構造で生成し、描画後に要素を移動する処理を削除する。
- **完了条件:** 一般級・個別支援級の切替、クラス名、色、教科、保存がDOM加工なしで動作する。

### R403 データ管理の新旧カード混在を解消

- **優先度:** P1
- **状態:** ⬜ 未着手
- **対象コード:** `app-data.js` の `renderDataExchange()`、`app-data-sync.js` の `wireDataSectionTabs()`、`styles.css` の `.data-advanced` と旧 `.panel`
- **実機根拠:** `IMG_3434.PNG`
- **問題:** 共通設定カードと旧 `<details class="panel">` が混在し、分類が見出し文字列の判定に依存する。
- **作業:** 同期・保存・入出力・復元を明示IDで分類し、選択タブと現在の操作だけを表示する。見出しの文言で分類しない。
- **完了条件:** 文言を変更してもタブ分類が壊れず、選択した操作の実行ボタンが初期画面に見える。

### R404 設定一覧の情報密度を調整

- **優先度:** P2
- **状態:** ⬜ 未着手
- **対象コード:** `app-settings-core.js` の `renderSettingsGuide()`、`styles.css` の設定開始・状態・カテゴリカード
- **実機根拠:** `IMG_3431.PNG`
- **作業:** 年度・クラス・人数の状態表示を1行へまとめ、カテゴリカードを最初の画面で3～4項目確認できる高さにする。
- **完了条件:** iPad横向きで設定カテゴリの主要4分野が一覧できる。

---

## 段階5: CSV・Excel取り込みの安全性向上

### R501 氏名と出席番号の不一致を要確認にする

- **優先度:** P2
- **状態:** ⬜ 未着手
- **対象コード:** `app-data-import.js` の `matchImportedStudent()` と取込プレビュー
- **問題:** 出席番号と氏名が別児童を示す場合でも氏名側へ自動確定する。
- **完了条件:** 番号・氏名の不一致は自動登録されず、両候補と不一致理由が表示される。

### R502 RFC準拠に近いCSV解析へ置換

- **優先度:** P2
- **状態:** ⬜ 未着手
- **対象コード:** `app-data-import.js` の `parseExternalTable()`、既存の `csv-export.js` または新規共通CSV処理
- **問題:** 引用符内の改行を先に行分割し、1セルを複数レコードとして扱う。
- **完了条件:** カンマ、タブ、引用符、引用符内改行、空欄、BOMを含む代表CSVを正しく読める。

### R503 取り込み済み判定をクラス単位にする

- **優先度:** P2
- **状態:** ⬜ 未着手
- **対象コード:** `app-data-import.js` の `importHash()` 使用箇所、`externalImportHistory`
- **問題:** クラスIDが判定キーに含まれず、同じ表を別クラスへ取り込めない可能性がある。
- **完了条件:** 同じクラスへの重複取込は防ぎ、別クラスへの正当な取込は許可する。

### R504 不明な状態を無言で未提出に変換しない

- **優先度:** P2
- **状態:** ⬜ 未着手
- **対象コード:** `app-data-import.js` の `importedStatus()` と取込プレビュー
- **完了条件:** 解釈できない状態は行番号・元の値とともに要確認になり、ユーザーが対応を選ぶまで保存されない。

---

## 段階6: コード構造・ヘルプ・資料の整理

### R601 削除・保存・再描画の共通関数を整備

- **優先度:** P2
- **状態:** ⬜ 未着手
- **対象コード:** `app-shell.js`、`app-records.js`、`app-grades.js`、`app-data-sync.js`
- **作業:** 記録削除、取消、保存中の二重操作防止、保存後の再描画を共通化する。同期対象になる削除と一時的な入力取消を区別する。
- **完了条件:** 同じ意味の操作が画面ごとに別実装されず、削除履歴と元に戻す動作が統一される。

### R602 ヘルプ文章の描画後置換を撤去

- **優先度:** P2
- **状態:** ⬜ 未着手
- **対象コード:** `app-help.js` の `normalizeHelpText()`、`normalizeVisibleTerms()`、各ヘルプ生成箇所
- **作業:** 置換後の文章を正本へ反映し、DOM描画後の文字列置換を削減する。
- **完了条件:** 用語変更が一か所で管理され、DOM構造や句読点の変更で説明更新が外れない。

### R603 Service Workerのキャッシュ削除範囲を限定

- **優先度:** P2
- **状態:** ⬜ 未着手
- **対象コード:** `sw.js` の `activate` 処理
- **問題:** 現在のキャッシュ名以外を同一オリジン上ですべて削除する。
- **作業:** `class-support-shell-` 接頭辞を持つ旧キャッシュだけを削除する。
- **完了条件:** 同じGitHub Pagesオリジンの別アプリ用キャッシュを保持し、旧クラス支援ツールキャッシュだけを更新する。

### R604 依存関係と責務を整理

- **優先度:** P2
- **状態:** ⬜ 未着手
- **対象コード:** `JS_DEPENDENCY_MAP.md`、`index.html`、多数のグローバル関数を持つ各JS
- **作業:** データ、同期、設定、日常入力の責務を明記する。直ちに全面モジュール化はせず、変更頻度と依存の高い処理から共通APIへ寄せる。
- **完了条件:** 主要機能について「保存元・描画元・設定キー・テスト」が依存表から追える。

### R605 開発資料を一つの正本へ統合

- **優先度:** P2
- **状態:** ⬜ 未着手
- **対象:** `CHATGPT_HANDOFF_v129_RELEASE.md`、`CODEX_CHECKPOINT_v129.md`、`IMPLEMENTATION_PLAN_v129.md`、`SCREEN_INVENTORY*.md`、本ロードマップ
- **作業:** v128参照、作業中と完了の混在、テスト同梱有無などの矛盾を除去する。本ロードマップを進捗の正本とする。
- **完了条件:** 現在版、完了状況、未確認事項、次の作業が一つの資料から判断できる。

---

## 段階7: 実機検証と正式リリース

### R701 PCブラウザ検証

- **優先度:** P0判定
- **状態:** ⛔ 保留
- **前提:** 段階1～4完了
- **確認:** 初回設定、29～36人名簿、表示設定、毎日・週宿題、小テスト、長いノート履歴、印刷、バックアップ・復元。

### R702 iPad Safari検証

- **優先度:** P0判定
- **状態:** ⛔ 保留
- **前提:** 段階1～4完了
- **確認:** 横向き・縦向き、教師用・児童用、固定フッター、入力カード、5分ロック、オフライン起動、再読み込み後の設定保持。

### R703 2台同期検証

- **優先度:** P0判定
- **状態:** ⛔ 保留
- **前提:** R103～R107完了
- **確認:** PCとiPadで別々に追加・更新・削除し、追加、更新、同時刻競合、削除承認、同期前復元、暗号化バックアップ復元を確認する。

### R704 リリース整合と判定

- **優先度:** P0判定
- **状態:** ⛔ 保留
- **対象:** `APP_VERSION`、`index.html` の資材番号、`sw.js` のキャッシュ名、`README.md`、ZIP名、リリースノート
- **完了条件:** 全自動テスト、JS構文検査、PC実機、iPad実機、2台同期が完了し、未確認事項が明記されている。ここで初めて正式リリースと判定する。

---

## 4. 推奨する実装順

1. R001 → R101
2. R102 → R103 → R104 → R105 → R106 → R107
3. R108 → R109 → R110
4. R201 → R202 → R203 → R204 → R205
5. R301 → R302 → R303 → R304 → R307
6. R401 → R402 → R403 → R404
7. R305 → R306
8. R501 → R502 → R503 → R504
9. R601 → R602 → R603 → R604 → R605
10. R701 → R702 → R703 → R704

同期・復元の修正中は、表示や画面構成の大規模変更を同じ作業単位へ混ぜない。各項目を一つずつ実装し、対象テストと既存6テストを通してから次へ進む。

## 5. 各更新で必ず行う確認

- 変更前後に `node tests/run-tests.js` を実行する。
- 全JavaScriptへ `node --check` を実行する。
- IndexedDBの既存ストア名・キーを変更しない。
- 暗号化封筒v1/v2、同期ペイロードv1、旧ノートsessionKeyを読み続ける。
- UI変更ではiPad横向きの最初の画面と、最終行が固定フッターに隠れないことを確認する。
- データ変更では、成功時だけstateと下書き状態を更新し、失敗時に元データが残ることを確認する。
- 実機未確認の項目は「完了」ではなく「実機確認待ち」とする。

## 6. 次に着手する項目

最初の実装は **R101 表示設定画面の実行時エラー修正** とする。画面停止を解消して小さな回帰テストを追加した後、データ消失リスクのある **R102～R107** を順番に進める。
