# v136 改修ロードマップ

v135リリース後に確認・提案された改修を、実装状況と優先度付きで整理する。

## 状態の定義

- **完了**：コード実装と静的検証が完了している。
- **仕様確定**：方針は確定しているが、コード未着手。
- **部分完了**：一部の表示・導線だけ対応済みで、全体整理が残っている。
- **未着手**：確認・提案段階で、実装していない。
- **実務中に確認**：特殊な再現テストは行わず、通常業務中に表示・操作上の問題が出た場合に対応する。

## v135時点で完了している基盤

| 状態 | 内容 | 主な場所 |
|---|---|---|
| 完了 | v135の版番号、Service Worker、CSS参照の整合 | `app.js`, `index.html`, `sw.js`, `styles.css` |
| 完了 | 固定機能アイコン、宿題達成アイコン🏅、表示設定の整理 | `app-core.js`, `app-settings-display.js` |
| 完了 | 行動記録カテゴリー初期化・カテゴリー変更導線 | `app-shell.js`, `app-behavior.js`, `app-records.js` |
| 完了 | 紙テストExcel取り込み入口・確認ダイアログの広幅化 | `app-grades.js`, `app-data-migration.js`, `styles.css` |
| 完了 | 紙テスト・ノート評価の一覧を児童カード形式へ変更 | `app-grades.js`, `app-records.js`, `styles.css` |
| 完了 | 席設定画面の条件折りたたみ・座席優先表示 | `app-seating.js`, `styles.css` |
| 完了 | 個別支援級児童メモ、各種ダイアログの広幅化 | `app-records.js`, `app-grades.js`, `styles.css` |
| 部分完了 | 同期・保存・復元の説明整理 | `app-data.js`, `app-data-sync.js`, `app-data-crypto.js` |
| 完了 | 回帰テスト・構文検査・v135リリース資料 | `tests/`, `V135_ROADMAP.md`, `V135_IMPLEMENTATION_REPORT.md` |

## v136の優先順位

| 優先度 | 状態 | 改修内容 | 主な場所・備考 |
|---|---|---|---|
| P0 | 完了 | ノート評価・小テスト履歴を入力画面と同じ児童一覧に統一 | `app-records.js`, `app-grades.js`, `app-shell.js`。新規記録は入力時の座席配置を保存 |
| P0 | 完了 | 履歴画面から「座席順／出席番号順」を削除 | `app-records.js`, `app-grades.js` |
| P0 | 完了 | 未評価・未入力の児童も履歴画面から追加入力可能にする | ノート評価・テスト得点に対応 |
| P0 | 完了 | 評価・点数の訂正を同じ児童カードから上書き保存 | ノート評価、手入力小テスト、紙テストに対応 |
| P1 | 完了 | 入力時の児童順・座席配置を新規記録へ保存 | 新規ノート評価・紙テスト・小テスト。既存記録は現在の配置を代替表示 |
| P0 | 完了 | 児童用ヘルプを削除し、必要最小限の案内だけにする | `app-shell.js`, `app-core.js` |
| P0 | 完了 | 児童用画面の「提出の操作は上の各ページで行います」を削除 | `app-shell.js`。表示後の文言補正も実施 |
| P0 | 完了 | 児童用文言を「先生」に統一 | `app-shell.js`, `app-core.js`。児童画面の表示文言を統一 |
| P0 | 完了 | 週宿題の「前の週の提出をしたいときは、先生に伝えてください。」へ変更 | `app-shell.js` の児童画面表示 |
| P1 | 完了 | 教師用ヘルプを「使い方」と「困ったとき・FAQ」に分離 | `app-help.js`, `styles.css` |
| P1 | 完了 | 初回チュートリアルを検索結果・FAQから分離 | `helpLandingHtml()`, `wireHelpModes()` |
| P1 | 完了 | 児童用の色、0.5秒、PINなど内部仕様の説明を削除 | `HELP_TOPICS.pupil`, `app-help.js` |
| P1 | 完了 | FAQ検索対象をFAQだけに限定し、チュートリアルを検索結果に混ぜない | `app-help.js` |
| P1 | 完了 | 週宿題管理ダイアログを横長・2列表示にする | `styles.css`。幅・2列・内部スクロールを実装 |
| P1 | 完了 | 週宿題を「表示中／非表示／終了済み」に分ける | `app-records.js` |
| P1 | 完了 | 35週程度を前提に検索・全件絞り込みを追加 | `app-records.js`, `styles.css` |
| P1 | 完了 | 表示切替と繰り返し終了・再開を別操作に分離 | `app-records.js` |
| P1 | 完了 | 選択時だけ操作バーを表示し、ダイアログ下部に固定 | `app-records.js`, `styles.css` |
| P1 | 完了 | 賞状タグを9観点＋「成長が見られた」に整理 | 新規入力の既定値と観点対応を実装 |
| P1 | 完了 | 「発表・表現」「よく発表した」を新規タグから外す | 過去記録は削除しない |
| P1 | 完了 | 賞状タグを追加のみの運用に変更 | 設定画面を追加専用にし、既存タグ・履歴は削除しない |
| P1 | 完了 | 自作タグに集計先を設定できるようにする | `app-settings-records.js`。集計しない選択にも対応 |
| P1 | 完了 | 賞状タグに関連観点を内部保存する | `behaviorCategories` と既定タグ対応表を保存 |
| P1 | 完了 | 行動記録と同じ期間で賞状記録を集計する | `app-behavior.js` に前期・後期・年間・日付指定の集計を追加 |
| P1 | 完了 | 「直接記録」と「賞状・称賛」を分けて表示する | 賞状集計を独立パネルで表示 |
| P1 | 完了 | 観点別件数から具体的な賞状・メモを開けるようにする | 集計カードから該当する賞状記録を開く |
| P2 | 完了 | 旧賞状タグを過去タグとして保持し、新規選択から除外 | データ削除は禁止。既存記録だけで参照可能 |
| P2 | 完了 | 新規タグの集計先に「集計しない」を追加 | 記念用タグなどに対応 |
| P2 | 完了 | 賞状・称賛の集計を所見素材へ反映 | `app-reports.js`。記録を選択して所見素材へ追加可能 |
| P2 | 見送り | 賞状記録を教師確認後に行動記録へ転記する導線 | 二重記録を避け、賞状集計と直接記録を別資料として確認する |
| P2 | 実務中に確認 | Safariの固定フッター、縦幅、ダークモード、ダイアログ見切れ | iPad Safariで通常利用中に確認 |
| P2 | 実務中に確認 | 特殊な同期不整合・削除競合・復旧異常 | 専用の再現テストは行わない |

## 賞状タグの確定初期値

賞状画面では行動カテゴリー名を見せず、自然な称賛文だけを表示する。

| 表示文 | 集計先 |
|---|---|
| 準備や身の回りを整えた | 基本的な生活習慣 |
| 元気に取り組んだ | 健康・体力の向上 |
| 自分から考えて行動した | 自主・自律 |
| 最後までやり切った | 責任感 |
| 工夫して取り組んだ | 創意工夫 |
| 友達と協力した | 思いやり・協力 |
| みんなのために働いた | 勤労奉仕 |
| 誰にでも公平に接した | 公正・公平 |
| ルールやみんなの物を大切にした | 公共心・公徳心 |
| 成長が見られた | 成長・挑戦 |

生命尊重・自然愛護は、日常の賞状として適切な場面が少ないため初期タグには含めない。必要になった場合は追加タグとして登録する。

## 期間集計の仕様

- 期間は行動記録と同じ「前期・後期・年間・日付指定」を使用する。
- 1人1日1賞状を基本とする。
- 1つの賞状で複数カテゴリを選んだ場合、各カテゴリに1件ずつ加算する。
- 同じカテゴリを重複計上しない。
- 「直接記録」と「賞状・称賛」は別件数で表示する。
- 賞状件数から行動評価や通知表評定を自動決定しない。
- 旧タグや集計先未設定のタグは、過去記録として保持する。

## 実装後の検証

### コード検査

- JavaScript全ファイルの構文検査
- 既存回帰テスト
- 賞状タグの追加・保存・再表示
- 期間集計の境界日（前期・後期・年度末）
- 同一日複数カテゴリの集計
- 旧タグを含む過去記録の表示
- 週宿題35週の絞り込み・表示切替
- 履歴画面で未評価児童を新規作成できること

### 実機確認

実機確認は、通常業務中に確認しやすい表示・操作に限定する。

- 児童用画面の短い文言
- 週宿題管理ダイアログの横幅と2列表示
- 賞状タグ選択の押しやすさ
- 行動記録期間集計の見やすさ
- ダークモード、Safariツールバー表示時の見切れ

## リリース目標

上記P0〜P1のコード改修と静的検証が完了した時点で **v136** とする。P2の所見素材連携や実務中に発生する特殊不具合は、v136後の追加改修として扱う。
