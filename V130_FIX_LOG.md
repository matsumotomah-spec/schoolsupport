# v130 修正記録

## 修正済み

- 最後のクラスを削除しても、次回起動時に全データを初期化しない。
- 同期ファイル内の新しい記録より古い削除履歴では、記録を削除しない。
- クラス単位のごみ箱を、バックアップと年度限定復元の対象に含める。
- 別端末で削除されたクラスは、承認後に名簿・記録とともに削除履歴を取り込む。
- 年度限定復元で、他年度でも在籍中の児童情報を上書きしない。
- 同一同期ファイルを再取り込みしても、同じ競合コピーを増やさない。
- 非同期保存後の下書き解除が `event.currentTarget` の失効で失敗する経路を修正。
- データ管理の分類で、見出し文言による判定を撤去。

## 検証済み

- `node tests/run-tests.js`
- 全 JavaScript の `node --check`

## 実機確認待ち

- iPad Safari と PC での実データ同期、削除承認、バックアップ復元。
- 最後のクラス削除後の再起動と、ごみ箱からのクラス復元。
